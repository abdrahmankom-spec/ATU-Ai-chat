# ✨ ATU Портал с ИИ‑помощником

**АТУ Портал** — демонстрационный мини‑портал учебного заведения, визуально представленный в виде "этажей". Пользователь перемещается между этажами через удобный интерфейс и получает доступ к нужной информации. Главная фишка — встроенный локальный ИИ‑помощник, полностью работающий в браузере (без сервера).

> ⚠️ **ВАЖНО**
>
> САЙТ ЗАПУСКАЕТСЯ С ФАЙЛА `run_local_server.bat`.
>
> ИИ‑модель после запуска сервера загружается ~1 минуту, поэтому чат ИИ недоступен сразу. Используемая модель **Phi‑2** — маленькая и служит только демонстрацией. Для реальных проектов требуется сервер, полноценная база данных и более мощные модели.

---

> 🔎 *Кратко:* локальная демонстрация возможностей WebLLM + RAG для образовательных интерфейсов.

---

## 📌 Содержание

1. [Особенности](#-особенности)
2. [Структура проекта](#-структура-проекта)
3. [Этажи (функции)](#-этажи)
4. [Технологии](#-технологии)
5. [Запуск](#-установка-и-запуск)
6. [ИИ‑помощник](#-ии‑помощник)
7. [Адаптивность и производительность](#-адаптивность-и-производительность)
8. [Лицензия](#-лицензия)

---

## 🚀 Особенности

* **3D параллакс эффекты** — многослойная анимация с глубиной
* **Интерактивные этажи** — навигация по 6 этажам с разным функционалом
* **Локальная авторизация** — хранение профилей в `localStorage`
* **Встроенный ИИ‑помощник** — WebLLM + RAG для ответов по порталу
* **Адаптивный дизайн** — десктоп / планшет / мобайл
* **Лифт навигация** — быстрый переход между этажами

---

## 📁 Структура проекта

```
ATU/
├── index.html
├── css/
│   ├── style.css
│   └── page.css
├── js/
│   ├── main.js
│   ├── page.js
│   └── bd.js
├── pages/
│   ├── floor-1.html
│   ├── floor-2.html
│   ├── floor-3.html
│   ├── floor-4.html
│   ├── floor-5.html
│   └── floor-6.html
└── media/
    ├── dashboard/
    ├── news/
    └── *.png, *.jpg
```

---

## 🎯 Этажи

* **6 этаж — Дашборд**: расписание, дисциплины, задания, журнал оценок
* **5 этаж — Новости**: новости, анонсы, объявления
* **4 этаж — Библиотека**: электронные ресурсы, рекомендации
* **3 этаж — Учебный план**: программы и дисциплины
* **2 этаж — Эдвайзер**: информация и контакты эдвайзера
* **1 этаж — Профиль**: настройки, личные данные, выход

---

## 🛠 Технологии

* **HTML5 / CSS3 / JS (ES6+)**
* **LocalStorage / IndexedDB** — локальное хранение данных
* **WebLLM / Transformers.js** — запуск модели и embeddings в браузере
* **WebGPU** (при поддержке браузера) — ускорение модели

---

## 🔧 Установка и запуск

**Требования:** Node.js 14+, современный браузер с поддержкой WebGPU (Chrome/Edge recent builds).

**Быстрый старт:**

```bash
# клонировать репозиторий
git clone <repository-url>
cd "ATU — ИИ"

# запустить (Windows)
run_local_server.bat
# или вручную
node server.js
```

Откройте: `http://localhost:3000`.

> ⚠️ Важно: WebLLM требует запуска через HTTP сервер и правильных заголовков
>
> * `Cross-Origin-Opener-Policy: same-origin`
> * `Cross-Origin-Embedder-Policy: require-corp`

Запуск `index.html` напрямую **не будет работать** для ИИ‑чата.

---

## 🤖 ИИ‑помощник

* **Модель (пример):** Qwen2.5-0.5B-Instruct (квантизация 4-bit) или Phi-2 в демо
* **RAG:** семантический поиск по блокам портала (embeddings)
* **Embeddings:** например `all-MiniLM-L6-v2`
* **Функции:** отвечает на вопросы, знает данные студента (ФИО, группа, оценки), помогает навигации, может выполнять простые команды по согласию (обновить страницу, открыть раздел и т.п.)

**Примечание:** при первом запуске модель загружается (примерно 1–3 минуты, зависит от интернета). Последующие запуски быстрее за счёт кеша в `IndexedDB`.

---

## 📱 Адаптивность и производительность

* Desktop — полный функционал с 3D эффектами
* Tablet — упрощённые анимации
* Mobile — мобильная версия без тяжёлых 3D

Оптимизации:

* `requestAnimationFrame` для анимаций
* кэширование DOM-элементов
* ленивые загрузки медиа

---

## 📝 Лицензия

Проект создан в оброзовательных целях как демонстрация для Алматинского Технологического Университета. 


---

## Полезные сниппеты

### Параллакс (фрагмент)

```javascript
function updateParallax(smoothScroll, scale) {
  bg.style.transform = `translate3d(0, ${smoothScroll / -6}px, 0)`;
  mid.style.transform = `translate3d(0, ${smoothScroll / -10}px, 0)`;
  top.style.transform = `translateX(-50%) translate3d(0, ${smoothScroll / -5.7}px, 0) scale(${scale})`;
}
```

### Плавный переход между этажами

```javascript
function gotoFloor(i) {
  const y = floors[i].offsetTop + 1;
  window.scrollTo({ top: y, behavior: 'smooth' });
}
```


